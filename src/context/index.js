import {createContext} from 'react';

export const PedidosContext   = createContext();
export const EnderecoContext  = createContext();
export const Editar           = createContext();

//export const PedidosAcompanhamentoContext = createContext({});
//export const observacao = createContext({});
